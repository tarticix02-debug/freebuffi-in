/**  * Yalnızca bu modlardaki PGN, gerçek oyunu tam ve doğru şekilde temsil eder.  * Diğer tüm varyantlar tahtayı chess.js hamle sistemi DIŞINDA (put/remove/  * forceActiveColor) değiştirir — bu mutasyonlar PGN'e yansımaz. Bu yüzden  * bu modların Game Review'a gönderilmesi YANLIŞ analiz üretir.  */ export const RATED_REVIEWABLE_MODES = ['classic', 'vs-computer'] as const;  export function isReviewableMode(mode: string): boolean {   return (RATED_REVIEWABLE_MODES as readonly string[]).includes(mode); }
