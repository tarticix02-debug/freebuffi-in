import { describe, it, expect } from 'vitest'; import { isReviewableMode } from '../src/utils/reviewability';  describe('Game Review gating', () => {   it('classic ve vs-computer incelenebilir', () => {     expect(isReviewableMode('classic')).toBe(true);     expect(isReviewableMode('vs-computer')).toBe(true);   });    it('varyant modları incelenemez (PGN güvenilmez olduğu için)', () => {     expect(isReviewableMode('jackpot')).toBe(false);     expect(isReviewableMode('uno')).toBe(false);     expect(isReviewableMode('teleport')).toBe(false);     expect(isReviewableMode('chaos')).toBe(false);   }); });
